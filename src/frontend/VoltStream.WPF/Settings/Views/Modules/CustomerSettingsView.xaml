<UserControl x:Class="VoltStream.WPF.Settings.Views.Modules.CustomerSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:VoltStream.WPF.Settings.ViewModels"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>

        <DataGrid Grid.Column="0" 
                  ItemsSource="{Binding Customers}" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False" 
                  IsReadOnly="True"
                  SelectedItem="{Binding SelectedCustomer}"
                  Margin="0,0,20,0"
                  BorderThickness="1"
                  BorderBrush="#DDDDDD"
                  Background="White"
                  RowHeight="35"
                  FontSize="14">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" />
                <DataGridTextColumn Header="Nomi" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="Telefon" Binding="{Binding Phone}" Width="120" />
                <DataGridTextColumn Header="Manzil" Binding="{Binding Address}" Width="150" />
                <DataGridTemplateColumn Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                        CommandParameter="{Binding}"
                                        Margin="0,0,5,0"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        ToolTip="Tahrirlash">
                                    <fa:IconImage Icon="Pen" Foreground="#0A003A" Width="14" Height="14"/>
                                </Button>
                                <Button Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                        CommandParameter="{Binding}"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        ToolTip="O'chirish">
                                    <fa:IconImage Icon="Trash" Foreground="Red" Width="14" Height="14"/>
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Column="1" 
                BorderThickness="1" 
                BorderBrush="#DDDDDD" 
                Background="#F9F9F9" 
                CornerRadius="5" 
                Padding="15"
                VerticalAlignment="Top">
            <StackPanel>
                <ContentControl Content="{Binding IsEditing}">
                    <ContentControl.Style>
                        <Style TargetType="ContentControl">
                            <Setter Property="ContentTemplate">
                                <Setter.Value>
                                    <DataTemplate>
                                        <TextBlock Text="Yangi qo'shish" FontSize="18" FontWeight="Bold" Margin="0,0,0,15" Foreground="#0A003A"/>
                                    </DataTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding}" Value="True">
                                    <Setter Property="ContentTemplate">
                                        <Setter.Value>
                                            <DataTemplate>
                                                <TextBlock Text="Tahrirlash" FontSize="18" FontWeight="Bold" Margin="0,0,0,15" Foreground="#0A003A"/>
                                            </DataTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ContentControl.Style>
                </ContentControl>

                <TextBlock Text="Nomi" Margin="0,0,0,5" Foreground="Gray"/>
                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" 
                         Padding="5" 
                         FontSize="14" 
                         Margin="0,0,0,15"
                         BorderBrush="#CCCCCC"/>

                <TextBlock Text="Telefon" Margin="0,0,0,5" Foreground="Gray"/>
                <TextBox Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" 
                         Padding="5" 
                         FontSize="14" 
                         Margin="0,0,0,15"
                         BorderBrush="#CCCCCC"/>

                <TextBlock Text="Manzil" Margin="0,0,0,5" Foreground="Gray"/>
                <TextBox Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" 
                         Padding="5" 
                         FontSize="14" 
                         Margin="0,0,0,15"
                         BorderBrush="#CCCCCC"/>

                <TextBlock Text="Izoh" Margin="0,0,0,5" Foreground="Gray"/>
                <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" 
                         Padding="5" 
                         FontSize="14" 
                         Height="60"
                         TextWrapping="Wrap"
                         VerticalContentAlignment="Top"
                         BorderBrush="#CCCCCC"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                    <Button Content="Bekor qilish" 
                            Command="{Binding CancelCommand}" 
                            Margin="0,0,10,0"
                            Width="100"
                            Height="30"
                            Background="Gray"
                            Foreground="White"
                            BorderThickness="0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="3">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    
                    <Button Content="Saqlash" 
                            Command="{Binding SaveCommand}" 
                            Width="100"
                            Height="30"
                            Background="#0A003A"
                            Foreground="White"
                            BorderThickness="0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="3">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>

                <TextBlock Text="{Binding Error}" Foreground="Red" Margin="0,10,0,0" TextWrapping="Wrap"/>
                <TextBlock Text="{Binding Success}" Foreground="Green" Margin="0,10,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
